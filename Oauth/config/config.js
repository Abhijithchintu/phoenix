var config = {
    local: {
        database: {
            host:       'localhost',
            user:       'root',
            password:   ''
        }
    },
    production: {
        database: {
            host:       'localhost',
            user:       'root',
            password:   ''
        }
    }
};

module.exports = config;
