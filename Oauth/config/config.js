var config = {
    local: {
        database: {
            host:       'localhost',
            user:       'root',
            password:   'Rakesh@123'
        }
    },
    production: {
        database: {
            host:       'localhost',
            user:       'root',
            password:   ''
        }
    }
};

module.exports = config;